import React from "react";

// Minimal placeholder Toaster so build does not fail.
// You can later replace this with the full shadcn Toaster implementation.
export function Toaster() {
  return null;
}

export default Toaster;
